[tool.poetry]
name = "teste"
version = "0.1.0"
description = "any"
authors = ["Geferson <gefersonjefreey@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
